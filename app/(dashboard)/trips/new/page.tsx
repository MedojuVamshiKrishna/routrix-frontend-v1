import PageWrapper from "@/components/layout/PageWrapper"

export default function NewTripPage() {
  return (
    <PageWrapper
      title="New Trip"
      description="Trips are normally created from a master order. Keep this route only for future exception handling."
    />
  )
}
