import PageWrapper from "@/components/layout/PageWrapper"

export default function InvoicesPage() {
  return (
    <PageWrapper
      title="Invoices"
      description="Monthly client invoices with one row per master order, visible advances, chargeable extras, and final balance."
    />
  )
}
